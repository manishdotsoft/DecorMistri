import { Box } from '@mui/material';
import CreateProjects from '../CreateProject/CreateProject';

const CreateProject = () => {
  return (
    <Box>
      <CreateProjects />
    </Box>
  );
};

export default CreateProject;
